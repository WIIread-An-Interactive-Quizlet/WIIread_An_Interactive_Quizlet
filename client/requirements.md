## Packages
framer-motion | Essential for game loop animations and smooth UI transitions
canvas-confetti | For celebration effects on high scores

## Notes
- Game loop implemented using React state and requestAnimationFrame (via framer-motion)
- Collision detection uses simple AABB (Axis-Aligned Bounding Box) logic
- Data collection form submits to /api/submissions
- Tailwind config needs to support 'Fredoka' and 'Quicksand' fonts
